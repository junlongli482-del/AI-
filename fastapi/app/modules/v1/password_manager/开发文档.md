# 📚 FastAPI模块化用户系统 - 完整技术文档

## 📋 项目概览

这是一个基于FastAPI的模块化用户认证系统，采用版本化架构设计，支持用户注册、登录、用户中心、密码管理等核心功能。

**项目信息：**
- 🏠 项目路径：`C:\A_XM\xm_3\vue3-fastapi-mysql_v1.0\fastapi`
- 🌐 服务端口：8100
- 🗄️ 数据库：MySQL `user_system`
- 📊 表前缀：`us_`
- 🏗️ 架构：模块化 + 版本化设计

---

## 🔗 接口文档 (API Documentation)

### 基础信息
- **服务地址**：http://localhost:8100
- **API版本**：v1
- **认证方式**：JWT Bearer Token
- **文档地址**：http://localhost:8100/docs

---

### 1️⃣ 用户注册模块 (user_register)

#### 1.1 用户注册接口

**接口地址：** `POST /api/v1/user_register/register`

**功能描述：** 新用户账号注册

**请求参数：**
```json
{
  "username": "string",     // 用户名，3-20字符，唯一
  "email": "string",        // 邮箱地址，唯一
  "password": "string"      // 密码，最少8位，包含字母和数字
}
```

**成功响应（201）：**
```json
{
  "message": "用户注册成功",
  "user": {
    "id": 1,
    "username": "testuser",
    "email": "test@example.com",
    "is_active": true,
    "created_at": "2025-01-20T10:30:00"
  }
}
```

**错误响应：**
- `400` - 用户名已存在：`{"detail": "用户名已被注册"}`
- `400` - 邮箱已存在：`{"detail": "邮箱已被注册"}`
- `422` - 数据验证错误

#### 1.2 检查用户名可用性

**接口地址：** `GET /api/v1/user_register/check-username/{username}`

**成功响应（200）：**
```json
{
  "available": true,
  "message": "用户名可用",
  "username": "testuser"
}
```

#### 1.3 检查邮箱可用性

**接口地址：** `GET /api/v1/user_register/check-email/{email}`

**成功响应（200）：**
```json
{
  "available": true,
  "message": "邮箱可用",
  "email": "test@example.com"
}
```

#### 1.4 模块测试接口

**接口地址：** `GET /api/v1/user_register/test`

**成功响应（200）：**
```json
{
  "message": "用户注册模块正常运行",
  "module": "user_register",
  "version": "v1",
  "endpoints": [...],
  "database_status": "正常 (用户总数: 5)"
}
```

---

### 2️⃣ 用户认证模块 (user_auth)

#### 2.1 用户登录接口

**接口地址：** `POST /api/v1/user_auth/login`

**功能描述：** 支持用户名/邮箱 + 密码两种登录方式

**请求参数：**
```json
{
  "username_or_email": "string",  // 用户名或邮箱地址
  "password": "string",           // 用户密码
  "remember_me": false           // 是否记住登录（可选）
}
```

**成功响应（200）：**
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "refresh_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...", // remember_me=true时才有
  "token_type": "bearer",
  "expires_in": 1800  // 30分钟，单位：秒
}
```

**错误响应：**
- `401` - 登录失败：`{"detail": "用户名/邮箱或密码错误"}`

#### 2.2 获取当前用户信息

**接口地址：** `GET /api/v1/user_auth/me`

**请求头：**
```
Authorization: Bearer <access_token>
```

**成功响应（200）：**
```json
{
  "id": 2,
  "username": "testuser",
  "email": "test@example.com",
  "is_active": true,
  "created_at": "2025-01-20T10:30:00"
}
```

#### 2.3 刷新访问令牌

**接口地址：** `POST /api/v1/user_auth/refresh`

**请求参数：**
```json
{
  "refresh_token": "string"  // 刷新令牌
}
```

**成功响应（200）：**
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "refresh_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer",
  "expires_in": 1800
}
```

#### 2.4 模块测试接口

**接口地址：** `GET /api/v1/user_auth/test`

---

### 3️⃣ 用户中心模块 (user_profile)

#### 3.1 获取用户资料

**接口地址：** `GET /api/v1/user_profile/me`

**请求头：**
```
Authorization: Bearer <access_token>
```

**成功响应（200）：**
```json
{
  "id": 2,
  "username": "testuser",
  "email": "test@example.com",
  "nickname": "我的昵称",
  "display_name": "我的昵称",
  "is_active": true,
  "created_at": "2025-01-20 10:30:00",
  "updated_at": "2025-01-20 11:00:00"
}
```

#### 3.2 更新用户昵称

**接口地址：** `PUT /api/v1/user_profile/nickname`

**请求头：**
```
Authorization: Bearer <access_token>
```

**请求参数：**
```json
{
  "nickname": "我的新昵称"  // 2-20字符，支持中英文数字，可为null清空
}
```

**成功响应（200）：**
```json
{
  "message": "昵称更新成功",
  "success": true,
  "user": {
    "id": 2,
    "username": "testuser",
    "nickname": "我的新昵称",
    "display_name": "我的新昵称"
  }
}
```

**错误响应：**
- `400` - 昵称已被使用：`{"detail": "该昵称已被其他用户使用"}`

#### 3.3 检查昵称可用性

**接口地址：** `GET /api/v1/user_profile/check-nickname/{nickname}`

**成功响应（200）：**
```json
{
  "available": true,
  "message": "昵称可用",
  "nickname": "测试昵称"
}
```

#### 3.4 退出登录

**接口地址：** `POST /api/v1/user_profile/logout`

**请求头：**
```
Authorization: Bearer <access_token>
```

**成功响应（200）：**
```json
{
  "message": "用户 我的昵称 已成功退出登录",
  "success": true
}
```

#### 3.5 模块测试接口

**接口地址：** `GET /api/v1/user_profile/test`

---

### 4️⃣ 密码管理模块 (password_manager)

#### 4.1 修改密码接口

**接口地址：** `POST /api/v1/password_manager/change-password`

**功能描述：** 用户修改密码，需要验证原密码

**请求头：**
```
Authorization: Bearer <access_token>
```

**请求参数：**
```json
{
  "current_password": "string",   // 当前密码
  "new_password": "string",       // 新密码，最少8位，包含字母和数字
  "confirm_password": "string"    // 确认新密码
}
```

**成功响应（200）：**
```json
{
  "message": "密码修改成功",
  "success": true,
  "user_info": {
    "id": 2,
    "username": "testuser",
    "display_name": "我的昵称"
  }
}
```

**错误响应：**
- `400` - 当前密码错误：`{"detail": "当前密码错误"}`
- `400` - 新密码与当前密码相同：`{"detail": "新密码不能与当前密码相同"}`
- `422` - 密码验证失败

#### 4.2 检查密码强度

**接口地址：** `POST /api/v1/password_manager/check-strength`

**请求参数：**
```json
{
  "password": "string"  // 要检查的密码
}
```

**成功响应（200）：**
```json
{
  "is_valid": true,
  "strength_level": "强",
  "requirements": {
    "min_length": true,
    "has_letter": true,
    "has_number": true,
    "has_special": false,
    "has_upper": true,
    "has_lower": true
  },
  "suggestions": []
}
```

#### 4.3 获取当前用户信息

**接口地址：** `GET /api/v1/password_manager/user-info`

**请求头：**
```
Authorization: Bearer <access_token>
```

**成功响应（200）：**
```json
{
  "id": 2,
  "username": "testuser",
  "email": "test@example.com",
  "display_name": "我的昵称",
  "created_at": "2025-01-20 10:30:00"
}
```

#### 4.4 模块测试接口

**接口地址：** `GET /api/v1/password_manager/test`

**成功响应（200）：**
```json
{
  "message": "密码管理模块正常运行",
  "module": "password_manager",
  "version": "v1",
  "endpoints": [
    "POST /change-password - 修改密码",
    "POST /check-strength - 检查密码强度",
    "GET /user-info - 获取用户信息",
    "GET /test - 模块测试"
  ],
  "database_status": "正常 (用户总数: 5)"
}
```

---

## 🏗️ 开发文档 (Development Documentation)

### 项目目录结构

```
fastapi/
├── app/
│   ├── core/                          # 核心组件
│   │   ├── __init__.py
│   │   ├── config.py                  # 配置管理（含JWT配置）
│   │   └── database.py                # 数据库连接
│   ├── modules/                       # 业务模块（版本化）
│   │   └── v1/                        # 版本1功能
│   │       ├── __init__.py
│   │       ├── user_register/         # 用户注册模块 ✅
│   │       │   ├── __init__.py
│   │       │   ├── models.py          # 用户数据模型
│   │       │   ├── schemas.py         # API序列化模型
│   │       │   ├── services.py        # 注册业务逻辑
│   │       │   ├── routes.py          # 注册API路由
│   │       │   └── dependencies.py    # 模块依赖注入
│   │       ├── user_auth/             # 用户认证模块 ✅
│   │       │   ├── __init__.py
│   │       │   ├── models.py          # 复用User模型
│   │       │   ├── schemas.py         # 登录请求/响应模型
│   │       │   ├── services.py        # JWT认证服务
│   │       │   ├── routes.py          # 登录API路由
│   │       │   └── dependencies.py    # JWT依赖注入
│   │       ├── user_profile/          # 用户中心模块 ✅
│   │       │   ├── __init__.py
│   │       │   ├── models.py          # 扩展User模型（含昵称）
│   │       │   ├── schemas.py         # 用户资料API模型
│   │       │   ├── services.py        # 用户中心业务逻辑
│   │       │   ├── routes.py          # 用户中心API路由
│   │       │   └── dependencies.py    # 模块依赖注入
│   │       └── password_manager/      # 密码管理模块 ✅
│   │           ├── __init__.py
│   │           ├── models.py          # 复用User模型
│   │           ├── schemas.py         # 密码管理API模型
│   │           ├── services.py        # 密码管理业务逻辑
│   │           ├── routes.py          # 密码管理API路由
│   │           └── dependencies.py    # 模块依赖注入
│   └── main.py                        # 应用入口（自动注册模块）
├── requirements.txt                   # 项目依赖
├── .env                              # 环境配置
└── README.md                         # 项目说明
```

### 技术架构

**核心技术栈：**
- **框架**：FastAPI 0.104.1
- **数据库**：MySQL + PyMySQL
- **ORM**：SQLAlchemy 2.0.23
- **认证**：JWT (python-jose[cryptography] 3.3.0)
- **密码加密**：SHA256 + 盐值
- **配置管理**：python-decouple 3.8

**架构特点：**
- ✅ **模块化设计**：每个功能独立模块
- ✅ **版本化管理**：支持多版本并存
- ✅ **自动注册**：新模块零配置自动发现
- ✅ **零修改原则**：新功能不修改现有代码
- ✅ **标准结构**：每个模块5个标准文件

**JWT配置：**
- 访问令牌有效期：30分钟
- 刷新令牌有效期：7天
- 加密算法：HS256

### 数据库设计

**表名：** `us_users`

**表结构：**
```sql
CREATE TABLE us_users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    email VARCHAR(100) NOT NULL UNIQUE,
    password_hash VARCHAR(255) NOT NULL,  -- 格式：盐值:哈希值
    nickname VARCHAR(50) DEFAULT NULL UNIQUE,  -- 用户昵称
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_username (username),
    INDEX idx_email (email),
    INDEX idx_nickname (nickname)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

**字段说明：**
- `id`：主键，自增
- `username`：用户名，3-20字符，唯一
- `email`：邮箱地址，唯一
- `password_hash`：密码哈希，格式：`盐值:SHA256哈希值`
- `nickname`：用户昵称，2-20字符，唯一，可为NULL
- `is_active`：账户状态，默认激活
- `created_at`：创建时间
- `updated_at`：更新时间

**显示名称逻辑：**
- 优先显示昵称（如果设置了）
- 昵称为空时显示用户名

### 已完成功能清单

#### ✅ 用户注册模块 (user_register)
- [x] 用户注册功能
- [x] 用户名唯一性验证
- [x] 邮箱唯一性验证
- [x] 密码强度验证
- [x] 实时可用性检查接口
- [x] 模块测试接口

#### ✅ 用户认证模块 (user_auth)
- [x] 双重登录方式（用户名/邮箱+密码）
- [x] JWT令牌生成和验证
- [x] 记住登录状态（刷新令牌）
- [x] 获取当前用户信息
- [x] 令牌刷新功能
- [x] 用户身份验证中间件

#### ✅ 用户中心模块 (user_profile)
- [x] 获取用户完整资料
- [x] 更新用户昵称
- [x] 昵称唯一性验证
- [x] 昵称可用性检查
- [x] 退出登录功能
- [x] 显示名称优先级逻辑

#### ✅ 密码管理模块 (password_manager)
- [x] 修改密码功能（验证原密码）
- [x] 密码强度检查和评估
- [x] 密码改进建议
- [x] 获取用户信息（用于密码修改页面）
- [x] 完整的安全验证流程

### 待开发功能清单

#### 🔄 系统扩展功能
- [ ] 用户头像上传模块
- [ ] 个人资料扩展字段
- [ ] 用户权限管理系统
- [ ] 操作日志记录模块
- [ ] 邮箱验证功能
- [ ] 忘记密码功能
- [ ] 账户锁定机制
- [ ] 登录历史记录

#### 🔄 管理后台功能
- [ ] 用户管理界面
- [ ] 系统监控面板
- [ ] 数据统计分析
- [ ] 系统配置管理

---

## 📋 继续开发指南 (Development Continuation Guide)

### 🎯 项目背景信息

我正在开发一个**模块化的FastAPI用户系统**，采用版本化的模块架构。目前已经完成了用户注册、用户认证、用户中心和密码管理四个核心模块，形成了一个完整的用户认证系统。

### 📊 当前项目状态

**项目信息：**
- 项目路径：`C:\A_XM\xm_3\vue3-fastapi-mysql_v1.0\fastapi`
- 服务端口：8100
- 数据库：MySQL，数据库名 `user_system`
- 表前缀：`us_`

**技术栈：**
- FastAPI + SQLAlchemy + MySQL
- JWT认证（python-jose）
- 密码加密（SHA256+盐值）
- 自动模块注册机制

**已完成模块：**
1. **用户注册模块**（`app/modules/v1/user_register/`）
2. **用户认证模块**（`app/modules/v1/user_auth/`）
3. **用户中心模块**（`app/modules/v1/user_profile/`）
4. **密码管理模块**（`app/modules/v1/password_manager/`）

### 🗄️ 数据库结构

**当前表结构：**
```sql
-- us_users 表（完整版本）
CREATE TABLE us_users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    email VARCHAR(100) NOT NULL UNIQUE,
    password_hash VARCHAR(255) NOT NULL,  -- 格式：盐值:哈希值
    nickname VARCHAR(50) DEFAULT NULL UNIQUE,  -- 用户昵称
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_username (username),
    INDEX idx_email (email),
    INDEX idx_nickname (nickname)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 🎯 系统功能完整性

**核心用户系统已完成：**
- ✅ 用户注册（用户名/邮箱唯一性验证）
- ✅ 用户登录（支持用户名/邮箱登录）
- ✅ JWT认证（访问令牌+刷新令牌）
- ✅ 用户中心（资料查看、昵称管理）
- ✅ 密码管理（修改密码、强度检查）
- ✅ 退出登录

**系统特性：**
- 🔐 完整的安全认证体系
- 📱 RESTful API设计
- 🏗️ 模块化架构
- 🔄 自动模块注册
- 📊 完整的错误处理

### 🏗️ 模块架构规范

**标准模块结构：**
```
app/modules/v1/{模块名}/
├── models.py          # 数据模型
├── schemas.py         # API序列化模型
├── services.py        # 业务逻辑
├── routes.py          # API路由
└── dependencies.py    # 模块依赖
```

**开发原则：**
- ✅ 新模块在 `app/modules/v1/` 下创建目录
- ✅ 遵循5个标准文件结构
- ✅ main.py 自动注册，无需修改
- ✅ 模块间通过API通信，避免直接依赖

### 🔧 配置文件状态

**app/core/config.py：**
```python
from decouple import config

class Settings:
    DATABASE_URL: str = config("DATABASE_URL")
    SECRET_KEY: str = config("SECRET_KEY")
    ALGORITHM: str = config("ALGORITHM", default="HS256")
    ACCESS_TOKEN_EXPIRE_MINUTES: int = config("ACCESS_TOKEN_EXPIRE_MINUTES", default=30, cast=int)
    REFRESH_TOKEN_EXPIRE_DAYS: int = config("REFRESH_TOKEN_EXPIRE_DAYS", default=7, cast=int)

settings = Settings()
```

**.env 配置：**
```env
DATABASE_URL=mysql+pymysql://root:ljl18420@localhost:3306/user_system
SECRET_KEY=your-secret-key-here-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7
```

### 🧪 测试数据

**测试用户：**
- 用户名：testuser
- 邮箱：test@example.com
- 密码：password123（或已修改的新密码）
- 昵称：我的昵称（如果已设置）

**JWT令牌获取：**
```bash
POST /api/v1/user_auth/login
{
  "username_or_email": "testuser",
  "password": "password123",
  "remember_me": true
}
```

### 📋 下一步开发建议

**可选扩展方向：**

1. **前端集成**
   - Vue3前端页面开发
   - 用户界面设计
   - API调用集成

2. **功能扩展**
   - 用户头像上传
   - 个人资料扩展
   - 邮箱验证功能
   - 忘记密码功能

3. **管理功能**
   - 管理员后台
   - 用户管理界面
   - 系统监控

4. **安全增强**
   - 登录频率限制
   - 账户锁定机制
   - 操作日志记录

### 📋 开发指导原则

**对于新对话的开发者：**
1. 根据开发者水平选择教学速度（慢/中/快）
2. 一次只开发一个模块，确保能跟上进度
3. 先提供完整的需求分析和架构设计
4. 逐步编写代码文件，每个文件都要详细解释
5. 提供完整的测试指导

**重要提醒：**
- main.py 无需修改，自动注册新模块
- 如需更新现有文件，先要求查看当前内容
- 遵循模块化原则，避免跨模块直接依赖
- 新模块的routes.py中不要设置prefix和tags（main.py中已设置）

---

## 🚀 快速启动命令 (Quick Start Commands)

### 服务器启动

```bash
# 进入项目目录
cd C:\A_XM\xm_3\vue3-fastapi-mysql_v1.0\fastapi

# 启动服务器
python -m app.main

# 服务器将在以下地址启动：
# http://localhost:8100
```

### API文档访问

```bash
# Swagger UI 文档
http://localhost:8100/docs

# ReDoc 文档
http://localhost:8100/redoc
```

### 模块测试命令

```bash
# 测试用户注册模块
curl http://localhost:8100/api/v1/user_register/test

# 测试用户认证模块
curl http://localhost:8100/api/v1/user_auth/test

# 测试用户中心模块
curl http://localhost:8100/api/v1/user_profile/test

# 测试密码管理模块
curl http://localhost:8100/api/v1/password_manager/test
```

### 完整功能测试流程

```bash
# 1. 用户注册
curl -X POST http://localhost:8100/api/v1/user_register/register \
  -H "Content-Type: application/json" \
  -d '{"username":"newuser","email":"new@example.com","password":"password123"}'

# 2. 用户登录
curl -X POST http://localhost:8100/api/v1/user_auth/login \
  -H "Content-Type: application/json" \
  -d '{"username_or_email":"testuser","password":"password123","remember_me":false}'

# 3. 获取用户资料（需要替换token）
curl -X GET http://localhost:8100/api/v1/user_profile/me \
  -H "Authorization: Bearer <your_access_token>"

# 4. 更新昵称（需要替换token）
curl -X PUT http://localhost:8100/api/v1/user_profile/nickname \
  -H "Authorization: Bearer <your_access_token>" \
  -H "Content-Type: application/json" \
  -d '{"nickname":"我的新昵称"}'

# 5. 修改密码（需要替换token）
curl -X POST http://localhost:8100/api/v1/password_manager/change-password \
  -H "Authorization: Bearer <your_access_token>" \
  -H "Content-Type: application/json" \
  -d '{"current_password":"password123","new_password":"newpass123","confirm_password":"newpass123"}'

# 6. 检查密码强度
curl -X POST http://localhost:8100/api/v1/password_manager/check-strength \
  -H "Content-Type: application/json" \
  -d '{"password":"testpassword123"}'
```

### 数据库操作命令

```sql
-- 查看用户表结构
USE user_system;
DESCRIBE us_users;

-- 查看所有用户
SELECT id, username, email, nickname, is_active, created_at FROM us_users;

-- 查看用户显示名称逻辑
SELECT 
    id, 
    username, 
    nickname,
    CASE 
        WHEN nickname IS NOT NULL THEN nickname 
        ELSE username 
    END AS display_name
FROM us_users;

-- 查看密码哈希格式
SELECT id, username, LEFT(password_hash, 50) as password_sample FROM us_users;
```

### 开发环境检查

```bash
# 检查Python环境
python --version

# 检查依赖包
pip list | grep fastapi
pip list | grep sqlalchemy
pip list | grep pymysql
pip list | grep jose

# 检查MySQL连接
mysql -