 
你说得对！我给的内容太多了，前端开发不需要那么多细节。让我重新生成一个**精简版**的前端接口文档。

---

# 📘 01-用户注册模块 - 前端接口文档

> **模块路径**：`/api/v1/user_register`  
> **接口数量**：2个

---

## 🔗 接口列表

### 1. 用户注册
- **地址**：`POST /api/v1/user_register/register`
- **功能**：创建新用户账号

### 2. 模块测试  
- **地址**：`GET /api/v1/user_register/test`
- **功能**：测试模块状态

---

## 📋 接口详情

### 1. 用户注册接口

**请求参数**
```json
{
  "username": "alice",        // 用户名，3-20字符，字母数字
  "email": "alice@qq.com",    // 邮箱地址
  "password": "password123"   // 密码，8-50字符，包含字母数字
}
```

**成功响应 (201)**
```json
{
  "id": 1,
  "username": "alice",
  "email": "alice@qq.com",
  "is_active": true,
  "created_at": "2025-01-27T10:30:45.123456"
}
```

**错误响应**
- `400` - `{"detail": "用户名已存在"}`
- `400` - `{"detail": "邮箱已存在"}`
- `422` - 参数格式错误

### 2. 模块测试接口

**响应**
```json
{
  "message": "用户注册模块正常运行",
  "module": "user_register",
  "version": "v1"
}
```

