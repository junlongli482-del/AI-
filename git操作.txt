好的！我来总结一下常用的 Git 命令，方便你日常使用。

## 📝 常用 Git 命令总结

### 1. 基础配置
```bash
# 设置用户名和邮箱
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"
# 查看配置
git config --list
```

### 2. 添加和提交
```bash
# 添加所有修改到暂存区
git add .
# 或添加特定文件
git add 文件名

# 提交到本地仓库
git commit -m "提交说明"

# 一次性添加并提交
git commit -am "提交说明"
```

### 3. 推送和拉取
```bash
# 推送到远程仓库
git push

# 拉取远程更新
git pull

# 强制推送（谨慎使用）
git push --force-with-lease
```

### 4. 分支操作
```bash
# 查看所有分支
git branch -a

# 创建新分支
git branch 分支名

# 创建并切换到新分支
git checkout -b 分支名

# 切换分支
git checkout 分支名

# 删除分支
git branch -d 分支名
```

### 5. 版本回退
```bash
# 查看提交历史
git log --oneline

# 临时查看老版本（不修改）
git checkout 版本号
# 查看完毕回到最新
git checkout master

# 安全回退（推荐）- 创建反向提交
git revert 版本号

# 彻底回退（危险）- 丢弃提交
git reset --hard 版本号
```

### 6. 清理未跟踪文件
```bash
# 预览要删除的文件
git clean -n -d

# 删除未跟踪的文件和文件夹
git clean -f -d

# 彻底清理（包括被忽略的文件）
git clean -f -d -x
```

### 7. 状态和差异查看
```bash
# 查看当前状态
git status

# 查看文件差异
git diff

# 查看暂存区的差异
git diff --staged
```

### 8. 撤销操作
```bash
# 撤销暂存的文件
git reset HEAD 文件名

# 撤销工作区的修改（危险）
git checkout -- 文件名

# 撤销最近一次提交（保留修改）
git reset --soft HEAD~1

# 撤销最近一次提交（丢弃修改）
git reset --hard HEAD~1
```

### 9. 储藏功能
```bash
# 储藏当前修改
git stash

# 查看储藏列表
git stash list

# 恢复最新储藏
git stash pop

# 删除所有储藏
git stash clear
```

## 🎯 常用工作流程

### 日常开发流程：
```bash
git pull                    # 拉取最新代码
git checkout -b feature-xx  # 创建功能分支
# 进行开发...
git add .                   # 添加修改
git commit -m "功能说明"     # 提交
git push origin feature-xx  # 推送分支
```

### 紧急修复流程：
```bash
git stash                  # 储藏当前工作
git pull                   # 更新代码
# 进行修复...
git add .                  # 添加修复
git commit -m "紧急修复"    # 提交
git push                   # 推送
git stash pop              # 恢复之前的工作
```

### 版本回退流程：
```bash
git log --oneline          # 查看历史
git revert 版本号           # 安全回退（推荐）
# 或
git reset --hard 版本号     # 彻底回退（谨慎）
git clean -f -d            # 清理未跟踪文件
```

这些命令覆盖了 90% 的日常 Git 使用场景，建议保存这个总结方便随时查阅！